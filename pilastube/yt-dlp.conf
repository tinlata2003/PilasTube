# PilasTube playback fallback configuration
# Prefer clients that currently avoid the common LOGIN_REQUIRED / bot-check
# response on handheld IPs. web_embedded -> tv -> android_vr.
--extractor-args=youtube:player_client=web_embedded,tv,android_vr
